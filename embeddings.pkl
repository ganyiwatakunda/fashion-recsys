import pickle
PATH = "/content/drive/MyDrive/"
pickle_in = open(PATH + "embeddings.pkl","rb",  "buffering=0"),( encoding='bytes')
pickle_in.seek(0)
embeddings = pickle.load(pickle_in)
print(embeddings)
